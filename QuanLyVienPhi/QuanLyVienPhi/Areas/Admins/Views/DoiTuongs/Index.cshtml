@model IEnumerable<QuanLyVienPhi.Models.DoiTuong>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admins/Views/Shared/Admin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SoThe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MienGiam)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SoThe)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MienGiam)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DoiTuongId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DoiTuongId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DoiTuongId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
